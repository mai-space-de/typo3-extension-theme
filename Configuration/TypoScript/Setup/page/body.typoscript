page {
    bodyTagCObject = COA
    bodyTagCObject {
        # Opening tag with id attribute
        10 = TEXT
        10 {
            data = page:uid
            wrap = <body id="page-|" class="
        }

        # Close class attribute and tag
        # (class content is appended by other extensions via stdWrap or COA items between 10 and 90)
        90 = TEXT
        90 {
            value = ">
        }
    }
}
